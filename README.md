# 🎨 PPT Compressor

现代化的 PowerPoint 压缩工具，采用 macOS Sequoia 设计风格的 Web 界面。

## ✨ 核心特性

- 🚀 **真正无损压缩** - 完全保留 PNG 透明度，使用 oxipng 无损压缩
- 🌐 **优雅 Web 界面** - 拖放上传，实时进度显示，流畅动画
- 📦 **一键打包部署** - 自动构建 macOS 应用，支持快速更新
- 💎 **4 种压缩档位** - 从完全无损到极致压缩，压缩率 15-85%

## 📦 快速开始

### 1. 安装依赖

```bash
# Python 依赖
pip3 install flask pillow werkzeug

# 可选：更好的 PNG 压缩效果
brew install oxipng
```

### 2. 选择使用方式

**方式一：macOS 应用（推荐）**
```bash
./build_app.sh                    # 构建并安装到 Applications
# 在启动台打开 "PPT Compressor"，浏览器会自动打开
```

**方式二：直接运行**
```bash
./run.sh                          # 或 python3 server.py
# 自动打开 http://127.0.0.1:5001
```

### 3. 使用压缩

1. 拖放 PPT 文件到界面
2. 选择压缩档位
3. 点击"开始压缩"
4. 实时查看进度
5. 下载压缩后的文件

## 📊 压缩档位说明

| 档位 | 压缩率 | 特点 |
|------|--------|------|
| **完全无损** | 15-30% | oxipng 真正无损，完整保留透明度 |
| **高质量** | 30-50% | 视觉无损，PNG 保留透明度 |
| **平衡** | 50-70% | 适度压缩，保留透明度（默认） |
| **激进** | 70-85% | 最大压缩，限制尺寸 1280px |

## 🔄 开发更新

```bash
./update_app.sh                   # 快速更新 Applications 中的应用
./build_app.sh                    # 完整重新构建（修改图标/配置时使用）
./run.sh                          # 开发模式运行，测试修改
```

## 💡 技术亮点

### 实时进度显示
- Server-Sent Events (SSE) 实时推送进度
- 流畅的进度条动画
- 分阶段显示：解压 → 压缩图片 → 重新打包

### macOS Sequoia 风格
- 玻璃态模糊效果（backdrop-filter）
- 动态渐变背景
- Apple 官方配色方案
- 响应式设计

### 压缩引擎
- 完全保留 PNG 透明度
- 支持 oxipng 无损压缩
- 智能图片格式转换
- 临时文件自动清理（1小时）

## 🎯 项目结构

```
PPT-compressor/
├── server.py                 # Flask Web 服务器 + SSE
├── ppt_compressor_v3.py     # 核心压缩引擎
├── templates/index.html     # Web UI 界面
├── static/
│   ├── style.css            # macOS Sequoia 样式
│   └── script.js            # 前端交互 + EventSource
├── build_app.sh             # 构建 macOS 应用
├── update_app.sh            # 快速更新脚本
└── create_icon.py           # 图标生成
```

## 🔧 常见问题

**应用打不开？**
- 右键点击应用 → "打开"
- 或在"系统偏好设置 > 安全性与隐私"中允许运行

**浏览器没有自动打开？**
- 手动访问：http://127.0.0.1:5001

**压缩效果不理想？**
- 尝试不同压缩档位
- 安装 oxipng：`brew install oxipng`

**应用更新后没变化？**
```bash
./update_app.sh              # 快速更新
killall Finder Dock          # 刷新系统
```

## 🛠 技术栈

- **后端**: Flask (Python 3), threading, queue
- **前端**: HTML5, CSS3, Vanilla JavaScript, EventSource API
- **压缩**: Pillow, oxipng
- **设计**: macOS Sequoia Design Language

## 📄 许可证

MIT License

---

Made with ❤️ for macOS Sequoia
